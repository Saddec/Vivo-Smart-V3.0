# 🕋 Vivo-Smart V3.0 (ESP32-S3)

**نظام ذكي متكامل لإدارة مواقيت الصلاة والأذان والتحكم المنزلي.**

مشروع مطور يعتمد على متحكم **ESP32-S3**، مصمم للعمل بكفاءة عالية باستخدام نظام المهام المتعددة (FreeRTOS) لإدارة الصوت والبيانات في وقت واحد.

## 🚀 المميزات التقنية (Technical Features)
- **نظام التوقيت العالمي:** حساب دقيق لمواقيت الصلاة بناءً على الموقع الجغرافي.
- **معالجة الصوت الاحترافية:** استخدام مكتبة `ESP32-audioI2S` لتشغيل الأذان بوضوح عالٍ عبر بروتوكول I2S.
- **برمجة نمطية (Modular Programming):** الكود مقسم لملفات مستقلة (الصوت، الويب، الجدولة) لسهولة الصيانة والتطوير.
- **لوحة تحكم ويب (Web Dashboard):** واجهة متكاملة لإدارة الإعدادات والملفات لاسلكياً.
- **إدارة الطاقة والذاكرة:** تحسين استهلاك الذاكرة واستخدام النواتين (Dual Core) لتوزيع العمليات.

## 📁 هيكلية المشروع (Project Structure)
- `AudioTask`: المسؤول عن فك تشفير وتشغيل ملفات الصوت.
- `PrayerTimesEngine`: محرك حساب المواقيت والتعامل مع الجداول الزمنية.
- `VivoWebServer`: إدارة خادم الويب وواجهة المستخدم.
- `GPIOManager`: التحكم في المداخل والمخارج والأنظمة المرتبطة.

## 🛠️ التجهيزات (Hardware)
- **Controller:** ESP32-S3-DevKitC-1 (8MB Flash).
- **Audio Interface:** I2S DAC (مثل MAX98357A).
- **Network:** WiFi Connectivity for Web Server & NTP Sync.

## ✅ الإصلاحات المطبقة (Applied Fixes)

### 🔧 إصلاحات وظائف صفحة الويب
تم إصلاح جميع المشاكل المتعلقة بعدم عمل الوظائف من خلال لوحة التحكم:

#### 1. **استكمال ملف `script.js`**
- ✅ إضافة جميع الدوال الناقصة (150+ سطر جديد)
- ✅ تنفيذ `updateClock()` - تحديث الوقت والتاريخ تلقائياً
- ✅ تنفيذ `fetchStatus()` - الحصول على حالة النظام
- ✅ تنفيذ `loadCountries()` و `onCountryChange()` - إدارة الدول والمدن
- ✅ تنفيذ `fetchPrayerTimes()` - حساب المواقيت
- ✅ تنفيذ `saveOffsets()` - حفظ إزاحات الصلاة
- ✅ تنفيذ وظائف الملفات (upload, delete, list)
- ✅ تنفيذ وظائف الجدولة والتشغيل
- ✅ تنفيذ وظائف GPIO والـ CSV والعيد

#### 2. **إضافة API Endpoints الكاملة في `VivoWebServer.cpp`**
تم إضافة 40+ endpoint جديد:

**الملفات:**
- ✅ `/api/files/list` - قائمة الملفات
- ✅ `/api/files/upload` - رفع الملفات (مع معالجة multipart/form-data)
- ✅ `/api/files/delete` - حذف الملفات
- ✅ `/api/files/mkdir` - إنشاء مجلدات

**الصوت والتشغيل:**
- ✅ `/api/audio/play` - تشغيل ملف صوتي
- ✅ `/api/audio/stop` - إيقاف التشغيل
- ✅ `/api/audio/volume` - التحكم بمستوى الصوت

**مواقيت الصلاة:**
- ✅ `/api/prayer/times` - الحصول على مواقيت اليوم
- ✅ `/api/prayer/calculate` - حساب المواقيت لدولة/مدينة
- ✅ `/api/prayer/offsets` - حفظ الإزاحات
- ✅ `/api/prayer/manual/*` - الوضع اليدوي

**الجدولة والـ GPIO:**
- ✅ `/api/scheduler/add` - إضافة جدولة
- ✅ `/api/scheduler/list` - قائمة الجدولات
- ✅ `/api/gpio/input/save` - ربط المدخلات
- ✅ `/api/gpio/output/save` - ربط المخارج
- ✅ `/api/gpio/schedule/*` - جدولة GPIO

**المميزات الإضافية:**
- ✅ `/api/eid/*` - وضع العيد والتكبيرات
- ✅ `/api/player/*` - مشغل الموسيقى والقوائم
- ✅ `/api/maghrib/*` - تنبيهات المغرب
- ✅ `/api/csv/*` - إدارة ملفات CSV
- ✅ `/api/startup/*` - تنبيهات بدء التشغيل
- ✅ `/api/location/*` - الدول والمدن
- ✅ `/api/system/ota` - تحديثات OTA

#### 3. **تحسينات معالجة البيانات**
- ✅ تحسين معالجة JSON في جميع الـ endpoints
- ✅ إضافة معالجة صحيحة للأخطاء (404, 400, 500)
- ✅ إضافة CORS headers لتجنب مشاكل التقاطع
- ✅ تحسين معالجة البيانات الضخمة (file uploads)

#### 4. **تحسينات واجهة المستخدم**
- ✅ إضافة تسجيل دخول مع كلمة مرور
- ✅ دعم مفتاح Enter في حقل البيانات
- ✅ تحسين رسائل الخطأ والنجاح
- ✅ تحديث تلقائي كل 3 ثوان
- ✅ أيقونات وألوان محسّنة

#### 5. **دالة `handleFileUpload` الكاملة**
- ✅ معالجة رفع الملفات بشكل آمن
- ✅ دعم ملفات كبيرة (streaming)
- ✅ معالجة الأخطاء والاستثناءات

## 🔐 تسجيل الدخول
كلمة المرور الافتراضية: **1234**

يمكن تغييرها من علامة التبويب "كلمة المرور" في لوحة التحكم.

## 📝 ملاحظات مهمة

### WiFi والشبكة
- الجهاز يتصل تلقائياً بـ SSID: "HONOR X9d" بكلمة مرور "123456789"
- يمكن تغيير الإعدادات من علامة "الشبكة"
- يدعم IP ثابت و DHCP

### الملفات
- جميع الملفات الصوتية توفر في بطاقة SD
- الملفات تُرفع من خلال واجهة الويب
- يدعم صيغ MP3 و WAV

### مواقيت الصلاة
- الحساب التلقائي حسب الموقع الجغرافي
- يدعم 3 طرق حساب
- يمكن الضبط اليدوي من علامة "ضبط يدوي"
- يدعم ملفات CSV لجداول محددة مسبقاً

---
*تم تطوير هذا المشروع بواسطة **Seddik Elzalaa**، فني كهربائي وتقني أنظمة تيار منخفض.*